import { StyleSheet } from 'react-native';

const iconFuncionalidadeStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 30,
  },
  iconContainer: {
    marginRight: 10,
  },
  textContainer: {
    marginTop: 2,
    color: '#F4F4F4',
    fontSize: 16,
    textAlign: 'center',
    width: 90,
  },
});

export default iconFuncionalidadeStyles;